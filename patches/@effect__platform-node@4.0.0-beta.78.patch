diff --git a/dist/NodeHttpServer.js b/dist/NodeHttpServer.js
index 7b14d02..0d2b6c7 100644
--- a/dist/NodeHttpServer.js
+++ b/dist/NodeHttpServer.js
@@ -100,7 +100,12 @@ export const make = /*#__PURE__*/Effect.fnUntraced(function* (evaluate, options)
   });
   const address = server.address();
   const wss = yield* Effect.acquireRelease(Effect.sync(() => new NodeWS.WebSocketServer({
-    noServer: true
+    noServer: true,
+    // Patch: upstreamed in Effect and available as a layer option in newer
+    // betas. Not Codex remains on beta.78 for now, so negotiate compression
+    // here until the dependency upgrade lands. Context takeover stays enabled
+    // so the compression window can be reused across streamed RPC frames.
+    perMessageDeflate: true
   })), wss => Effect.callback(resume => {
     wss.close(() => resume(Effect.void));
   })).pipe(Scope.provide(scope), Effect.cached);
diff --git a/src/NodeHttpServer.ts b/src/NodeHttpServer.ts
index 783d3d4..80857bf 100644
--- a/src/NodeHttpServer.ts
+++ b/src/NodeHttpServer.ts
@@ -134,7 +134,17 @@ export const make = Effect.fnUntraced(function*(
   const address = server.address()!
 
   const wss = yield* Effect.acquireRelease(
-    Effect.sync(() => new NodeWS.WebSocketServer({ noServer: true })),
+    Effect.sync(() =>
+      new NodeWS.WebSocketServer({
+        noServer: true,
+        // Patch: upstreamed in Effect and available as a layer option in newer
+        // betas. Not Codex remains on beta.78 for now, so negotiate compression
+        // here until the dependency upgrade lands. Context takeover stays
+        // enabled so the compression window can be reused across streamed RPC
+        // frames.
+        perMessageDeflate: true
+      })
+    ),
     (wss) =>
       Effect.callback<void>((resume) => {
         wss.close(() => resume(Effect.void))
