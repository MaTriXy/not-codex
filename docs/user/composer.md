# Composer attachments

You can paste or drag images into the web and desktop composer. Images that exceed the provider
attachment limit are safely resized before they are added.

HEIC and HEIF photos are converted to JPEG automatically. Conversion is bounded by source size and
decoded pixel count so unusually large or malformed files are rejected before they can exhaust the
browser tab.
